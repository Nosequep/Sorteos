describe('Buscar sorteo por id', () => {
    test('suma 1+2', () => {
        expect(1 + 2).toBe(3);
    });
    
    test('resta 1-2', () => {
        expect(1 - 2).toBe(-1);
    });
});

describe('Buscar sorteos por título', () => {
    test('suma 1+2', () => {
        expect(1 + 2).toBe(3);
    });
    
    test('resta 1-2', () => {
        expect(1 - 2).toBe(-1);
    });
});
